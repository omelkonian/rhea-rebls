\begin{tikzpicture}
  \matrix{
  	\node[dataflow] (A) {filter $p_1$}; \&
  	\node[dataflow] (B) {exists $p_2$}; \\
  };
  \draw[to] (A) -- (B);
  \draw[opt, <->] (B) to[bend right] (A);
\end{tikzpicture}
