\begin{tikzpicture}
  [every matrix/.append style={row sep=0.05cm}]
  \matrix{
    \& \node[dataflow] (map) {\small map $f$}; \\
    \node[dataflow] (ini) {\small 1..10}; \\
    \& \node[dataflow] (fil) {\small filter $g$}; \\
  };
  \draw[to] (ini) -- (map);
  \draw[to] (ini) -- (fil);
\end{tikzpicture}
