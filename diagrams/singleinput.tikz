\begin{adjustbox}{width=.5\columnwidth}
\begin{tikzpicture}
  \matrix{
    \node[dataflow] (just) {\small just [1, 2, 3]}; \\
    \node[dataflow] (map) {\small map \{$x + 1$\}};  \\
  };
  \draw[to] (just) -- (map);
\end{tikzpicture}
\end{adjustbox}
